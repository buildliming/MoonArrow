# MoonArrow 生态建设比赛：项目完善与交付计划

编制日期：2026-09-24。适用仓库：当前目录 `D:\moon-build`，模块 `shunge/arrow`。

本方案以当前代码和本地检查为基线。目标是把现有 Arrow IPC 子集实现建设为可以被其他 MoonBit 项目稳定集成的数据交换库：功能有深度、接口有契约、兼容性有独立验证、工程基础可持续维护，并使手写核心 MoonBit 有效代码超过 4000 行。

本文件是后续实施方案。比赛名称、截止日期和官方代码计数规则尚未提供；以下工期、版本号和验收指标均为项目内部建议，不代表比赛规则。确定赛程后调整日历安排，保持质量验收要求。

## 1. 现状与差距

### 1.1 已有基础

当前版本为 `0.1.0`，基线提交为 `20d83e5`。已经具备：

- 纯 MoonBit 实现，无 Arrow C/C++ 运行时依赖。
- Null、Boolean、Int32、Int64、Float64、Utf8、Binary 七种类型。
- IPC stream/file 读写、内存中逐批读取、文件批次索引访问。
- 空值、非空字段校验、schema/field 元数据、解析资源限制。
- 独立 PyArrow fixture、畸形输入与字节变异测试、Native/JS 互操作驱动。
- README、格式契约、贡献指南、变更记录和 Linux/Windows CI 配置。

本次本地核验结果：

| 项目 | 2026-09-24 核验结果 |
| --- | --- |
| 工具链 | moon `0.1.20260827` |
| `moon check --target all --deny-warn` | 通过 |
| `moon test --target all` | native、js、wasm、wasm-gc 各 18 项通过 |
| PyArrow 独立互操作 | 历史文档记录 Native/JS 各 80 项断言；本次未重跑 |
| 远端 CI、发布状态 | 本次未查询，不能以本地配置代替远端成功记录 |
| 性能与实际下游集成 | 当前未见可复现的性能报告与独立下游接入验收材料 |

核心代码统计仅纳入五个生产文件，不纳入测试、命令行、生成接口或文档：

| 文件 | 物理行数 | 非空且非整行注释行数 |
| --- | ---: | ---: |
| `binary.mbt` | 313 | 252 |
| `types.mbt` | 212 | 164 |
| `metadata.mbt` | 284 | 263 |
| `columns.mbt` | 325 | 316 |
| `ipc.mbt` | 298 | 258 |
| 合计 | **1432** | **1253** |

后一个口径是本次直接按文本统计的基线，包含括号等结构行，不等于 AST 语句数。后续用固定脚本持续生成相同口径报告，并另行兼容官方规则。

### 1.2 优先解决的问题

1. **真实数据覆盖偏窄**：缺少其他数值、时间、嵌套和字典类型，限制数据库结果、事件记录和分类数据接入。
2. **公共数据可变**：schema 和列中的数组可被调用方修改；现有 writer 会复核，但 reader 的 schema 有使用约束，需要明确所有权和失效规则。
3. **批次构建和加工不便**：缺少 builder、投影、切片、筛选、拼接等复用能力，下游容易重复实现。
4. **流式能力有限**：当前 reader 持有完整输入，writer 返回完整输出；不能据此宣称网络增量处理或有限内存处理大文件。
5. **工程证据不足**：需要工具链锁定、API 变更检查、测试矩阵报告、性能基线、发布演练和真正的外部接入案例。
6. **测试驱动不能代替生产 I/O**：当前 `cmd/interop` 使用命令行十六进制传输，仅适合小 fixture；大数据示例需要文件或二进制标准输入输出。

## 2. 最终交付目标与范围

### 2.1 必须完成的验收目标

| 维度 | 完成标准 | 交付证据 |
| --- | --- | --- |
| 稳定复用 | 独立消费项目能安装、构造批次、读写、处理错误；公开 API 有契约与迁移说明 | 消费项目、可运行示例、生成接口差异 |
| 基础类型 | 在现有类型上补齐 Int8/Int16、UInt8/16/32/64、Float32、Date32/64、Timestamp、Duration | 类型支持矩阵和跨语言 fixture |
| 嵌套结构 | 支持 List、Struct 及二者组合，递归深度与累计元素数受限 | 多层嵌套、空值和畸形输入测试 |
| 字典编码 | 完成明确支持范围内的字典读写和跨批次状态；stream/file 边界分别验证 | 字典状态测试与 PyArrow 双向验证 |
| 易用性 | builder、按索引投影、slice、take、filter、concat 可实际用于示例 | 公共 API 文档和语义测试 |
| 增量处理 | 分块输入 decoder、逐批输出 writer，可释放已消费数据，具有明确资源上限 | 任意切块测试、内存增长实验 |
| 可靠性 | 四后端持续通过；支持范围内的外部 fixture 双向验证；畸形输入返回受检错误 | CI、fixture 清单、回归报告 |
| 核心代码 | 手写生产 MoonBit 有效代码 **大于 4000 行**，建议留出余量达到 4600–5500 行 | 固定计数脚本与逐文件清单 |
| 性能 | 发布可重现的吞吐量、延迟、输出大小和内存报告 | 基准脚本、原始结果、环境信息 |
| 生态价值 | 至少两个完整工作流示例、一个独立消费项目，并争取一次实际使用反馈 | 示例、接入记录、反馈与处理记录 |

表中的目标均为待实施内容。实际用户反馈属于外部依赖：可以主动邀请试用；若未取得，交付时如实说明，不将自测描述为外部采纳。

### 2.2 扩展池与明确边界

- 核心目标完成后再考虑：FixedSizeBinary、FixedSizeList、Map、Decimal128、LargeUtf8/LargeBinary、投影解码。
- 压缩单独立项：先评估纯 MoonBit LZ4/Zstd 实现或依赖的成熟度、许可、后端支持和维护成本，再确定是否支持某一种 codec。
- 本轮不承诺完整 Arrow、完整 Feather V2、Parquet、Arrow Flight、SQL 引擎、线程调度器、mmap 或端到端零拷贝。
- 本轮时间类型负责整数值、单位和时区元数据的正确交换，不实现时区数据库或隐式时区转换。
- 扩展类型元数据的保留不等于支持其语义，文档继续明确这一点。

## 3. 技术架构与接口演进

### 3.1 分层原则

按“字节与边界检查 → Arrow 元数据 → 列布局 → 批次与字典 → IPC 状态 → 便捷 API”组织代码。底层模块不依赖命令行、Python、文件系统或网络。

初期保留 `shunge/arrow` 作为主要入口，先在根包内按职责拆分文件，减少大规模迁移。确认依赖关系后，再为确有独立使用价值的部分建立子包；跨包所需内部符号和兼容性成本须先评估，不为目录数量拆包。

建议文件布局如下，具体文件名可随实现调整：

```text
types.mbt / schema.mbt / errors.mbt / limits.mbt
binary.mbt / bitmap.mbt / flatbuffer.mbt
metadata_schema.mbt / metadata_message.mbt / metadata_footer.mbt
columns_primitive.mbt / columns_temporal.mbt / columns_nested.mbt
dictionary.mbt / builders.mbt / batch_ops.mbt
ipc_stream.mbt / ipc_file.mbt / incremental.mbt
cmd/main/ / cmd/interop/ / cmd/inspect/ / cmd/transform/
examples/ / bench/ / tools/ / docs/
```

生产文件保留 `///|` 块风格；公开 API 使用文档注释，弃用入口集中于 `deprecated.mbt`。新增文件和包应遵循各自目录的 MoonBit 配置与测试约定。

### 3.2 先落实的设计决策

1. **类型表示**：分别表达逻辑类型参数与列数据，Date/Timestamp 不以普通 Int64 冒充；UInt64 全程保持精度，不经 JS Number 中转。
2. **嵌套表示**：List 采用 offsets、父有效位和子列；Struct 采用父有效位和子字段/子列。避免逐单元格通用 JSON 值表示，递归处理 node/buffer 游标。
3. **可变性**：明确构造器是否复制数组、reader 是否保存独立 schema、批次操作是否产生独立结果。优先给出安全默认值，复制成本进入基准。
4. **错误与预算**：保持 Invalid、Unsupported、LimitExceeded 的分类，补充字段路径/批次上下文；递归、字典和增量输入使用累计预算，采用先检查再分配的流程。
5. **操作语义**：固定 slice 越界策略、take 重复索引行为、filter 空值处理、concat schema 相容规则；重复字段名时默认用索引消除歧义。
6. **状态隔离**：字典状态属于单个 reader/writer；增量 decoder 明确等待更多输入、完整消息、EOS、失败终态以及最终 EOF 的区别。
7. **I/O 隔离**：核心 API 面向 bytes/chunks；Native 文件和 JS 环境 I/O 放在适配层。接入依赖前做最小验证，避免让核心库引入平台专属运行时。

新增公开 enum 变体可能破坏下游穷举匹配，调整公开结构体字段也可能破坏构造代码。因此不承诺所有扩展都源码兼容。内部版本建议为 `0.2`（类型与接口）、`0.3`（嵌套/字典）、`0.4`（增量能力），每次发布说明实际破坏性变更，稳定性成熟后再讨论 `1.0`。

## 4. 分阶段任务、依赖与验收

估算按一名熟悉项目的开发者投入 **48–64 个有效工作日**，约 10–13 个全职工作周。兼职需按实际投入折算。以下按串行主线估算，不假定已有团队；每阶段以验收通过为结束条件。

### P0：建立可重复基线（3–4 日）

- 增加生产源码计数脚本，固定目录白名单和排除项，输出逐文件结果。
- 重跑 Native/JS PyArrow 互操作；保存 fixture 种子、版本、命令及结果。
- 锁定一个已验证的 MoonBit 工具链与依赖基线；另外设工具链升级验证通道。
- 梳理公开 API、可变性和错误行为，建立 `docs/API_POLICY.md` 与决策记录。
- 完善 CI：生成接口漂移检查、格式检查、文档示例、测试结果归档。
- 建立 benchmark 最小驱动，先记录当前实现的性能，不提前设绝对速度目标。

**验收**：干净检出可按文档重现；计数结果复核为 1253；生成接口无意外变化；四后端测试和两后端互操作均有记录。确认哪些 CI 已在远端执行。

### P1：基础类型扩展与布局整理（6–8 日，依赖 P0）

- 提取共享位图、固定宽度值读写、checked arithmetic 与布局遍历逻辑。
- 补齐上述整数、Float32 和时间类型；同步 schema 编解码与 Column API。
- 时间值保存单位和可选 timezone，明确 Date64 的合法值约束及不支持语义。
- 保留现有 API 的合理入口，对必要破坏性变更提供迁移示例。
- 为每类增加边界值、全空、空批次、切片来源、非法缓冲区测试。

**验收**：每种新增类型都能经 PyArrow → MoonBit → PyArrow 验证 stream/file；UInt64 极值和 Float32 特殊位模式正确；四后端结果一致。不能用强制转换后数值相等掩盖类型、单位或精度差异。

### P2：构建与批次复用 API（5–7 日，依赖 P1）

- 增加基础列 builder：追加值/空值、容量管理、finish、错误处理。
- 实现按索引投影、slice、take、filter、concat，优先有明确下游用途的操作。
- 明确空 schema、零列非零行、多批次、schema 元数据和所有权行为。
- 建立独立消费项目，使用公共入口完成“构建 → 加工 → IPC 导出”。

**验收**：越界与类型错误有受检错误；输出长度、空值和元数据符合约定；修改原输入不会意外改变承诺独立的结果。避免隐式做窄化转换或自动 schema 合并。

### P3：List/Struct 与递归验证（7–9 日，依赖 P1、P2）

- 扩展 Field/DataType 和列表示，实现递归 schema、node、buffer 编解码。
- 区分 null list、空 list、含 null 元素的 list；分别处理 Struct 的父空值与子空值。
- 覆盖 List<Struct<...>>、Struct<List<...>> 和多层 List。
- 将深度、字段总数、子元素总数和累计字节预算纳入 ReadLimits 或兼容的新配置。
- 为 builder 和批次操作补齐嵌套支持，不留下同名 API 的隐式支持缺口。

**验收**：支持范围内的递归组合具有外部 fixture；错误 offsets、子数组长度不匹配、预算边界、深层输入均有回归测试；超限在大数组分配前返回错误。

### P4：字典编码与跨批次状态（7–9 日，依赖 P3）

- 实现字典 schema 信息、DictionaryBatch 消息、ID 注册表与索引校验。
- 第一层范围固定为整数索引、Utf8/Binary 字典值；其他字典值类型按支持矩阵显式拒绝或另行扩展。
- 分别定义 stream 的字典初始化、delta 和 replacement，以及 file 的字典索引与读取规则；不能直接复用一个未经验证的状态策略。
- 明确随机读取批次需要的字典状态构建方式，并记录初始化时间/内存成本。
- 批次操作须定义字典一致、重映射或解码后的行为；不静默混用不同字典的索引。

**验收**：重复值、空值、多批次、缺失 ID、类型冲突、越界索引和不支持的更新模式均有测试。读写两端对支持能力一致，PyArrow 外部验证通过；不支持的组合明确返回 Unsupported。

### P5：增量输入、逐批输出与实用工具（8–11 日，依赖 P4）

- 增加分块输入 decoder，允许前缀、metadata、body 任意位置被切开。
- 引入最终 EOF 操作，区分“等待后续字节”和“输入已结束但被截断”。
- 增加逐批 writer，向调用方产出可及时消费的字节块；file writer 保留 footer 必需的索引信息，并限制其增长。
- 增加未消费输入、单帧、字典和输出缓冲预算；明确回调/消费端报错后的状态。
- 保留完整 Bytes 便捷接口，优先复用相同编解码实现，减少行为分叉。
- 增加 `inspect`、`validate`、stream/file 转换工具，以及真实文件 I/O 示例。
- 为互操作驱动增加文件传输方式，解除命令行长度限制。

**验收**：小 fixture 枚举全部单切分点并测试逐字节输入；较大 fixture 使用固定种子的随机切分。EOS 后额外数据、截断和失败终态可复现。消费者及时释放批次时，内存不随已处理批次数无限增长；文档注明最大帧、字典和文件索引仍占用内存。

### P6：性能、生态示例与发布材料（7–9 日，依赖 P5）

- 完成 Native/JS 编解码基准，分析数据复制、位图构造、分配和字符串编码热点。
- 仅对测量证实的瓶颈优化，记录优化前后的时间、内存及正确性结果。
- 示例 A：Python 生成事件表 → MoonBit 投影/过滤/分批处理 → Arrow 导出 → Python 校验。
- 示例 B：含 List/Struct 和字典列的数据 → 分块读取 → 批次处理 → 重新写出；提供 JS 或浏览器消费演示，按实际宿主标注支持范围。
- 更新中英文入口文档、类型矩阵、错误处理、迁移指南、架构说明和性能报告。
- 准备发布 dry-run、下游安装演练、版本说明、参赛说明和可重复演示脚本。

**验收**：核心有效代码超过 4000 行；示例从干净环境可运行；核心门禁通过；性能原始结果完整；支持范围与实际实现一致。正式发布和比赛提交作为之后的独立执行任务。

## 5. 核心代码规模预算

以下是最终生产实现的职责预算，**含现有代码，每块只计一次**；用于识别建设深度与工作量，不作为逐文件凑行数指标。

| 职责 | 最终有效代码参考值 | 实质内容 |
| --- | ---: | --- |
| 字节、位图、FlatBuffer、边界检查 | 550 | 多宽度数值、位图、偏移、预算、安全布局访问 |
| 类型、schema、校验、错误和 limits | 650 | 时间参数、嵌套结构、所有权与错误契约 |
| 列编码与解码 | 850 | 基础、时间、嵌套布局及空值语义 |
| 元数据与 stream/file 协议 | 750 | schema/message/footer、索引及协议校验 |
| 字典 | 500 | ID、更新、索引与跨批次状态 |
| builder 与批次操作 | 700 | 构建、投影、切片、筛选、选择、拼接 |
| 增量 decoder/writer | 600 | 分块状态机、输出、终止与资源控制 |
| 合计参考 | **4600** | 比当前 1253 行净增约 3347 行 |

计数要求：

- 只计自行编写、实际被库编译使用的生产 `.mbt`；按职责白名单纳入新文件。
- 排除 `_test.mbt`、`_wbtest.mbt`、`cmd/`、`examples/`、`bench/`、生成代码、依赖目录、脚本、文档和 `.mbti`。
- 同时输出物理行、空行、注释行、有效代码行；处理块注释时采用明确可复现的规则，并记录脚本版本。
- 不通过重复实现、无调用死代码、拆短语句、复制类型模板或引入无关功能扩大规模。
- 每阶段记录实际净增和删除量。若最终不足 4001 行，优先从已验证的下游需求中选择扩展池任务；不为了行数推迟合理抽象或删除冗余。

## 6. 质量与基础设施门禁

### 6.1 每次功能提交

- 同步实现、公共接口、支持矩阵、文档示例及回归测试。
- 稳定数值用断言；浮点序列化关注位模式；结构调试输出用 Debug/debug_inspect。
- 新类型必须覆盖合法输入、边界输入、畸形输入和独立实现生成的输入。
- 最后执行 `moon info`、`moon fmt`，检查 `.mbti` 是否只有预期变化；重新运行适当测试。

标准发布候选验证命令：

```text
moon check --target all --deny-warn
moon test --target all
python tools/interop.py --target native
python tools/interop.py --target js
moon info
moon fmt
moon fmt --check
git diff --check
```

CI 使用检查模式防止悄悄改源码，并检查 `moon info` 后生成接口是否发生未提交变化。性能任务和较大语料任务可放定时或手动通道，常规 PR 保持可接受耗时。

### 6.2 测试建设

| 层次 | 必须验证的内容 |
| --- | --- |
| 单元与边界 | 数值极值、位图边界、offset 溢出、空列、全空、零列非零行 |
| 协议状态 | framing、EOS/EOF、footer、schema 一致、字典顺序、终态 |
| 外部兼容 | PyArrow 输入读取、MoonBit 原生输出读取、重写后完整校验 |
| 组合性质 | 切片再拼接、投影行数不变、filter/take 结果与参考结果一致 |
| 恶意/错误输入 | 截断、重复引用、深层嵌套、超限分配请求、无 panic/无限循环 |
| 下游契约 | 独立包只用公开 API 构建与运行；旧版消费示例用于迁移验证 |

建议最终形成至少 100 个有独立语义的 MoonBit 测试案例、200 个可追踪的互操作场景；这些数量是补充指标，同一场景跨后端重复运行不重复计数。不得用增加无意义断言替代覆盖缺口。

先建立覆盖率基线，再以核心有效行覆盖率不低于 85% 为内部目标；若工具链对某后端统计有限，说明统计口径。解析入口、边界检查、字典和增量状态迁移另建人工用例清单，覆盖率不能代替这些验证。

fixture 记录生成版本、随机种子和预期结果；失败样本最小化后进入固定语料。PyArrow 基线版本保留，另设新版本兼容任务，不能只使用库自己的 writer 验证 reader。

### 6.3 性能实验设计

- 数据集：纯数值、可空混合列、字符串/二进制、嵌套、低/高基数字典。
- 规模：1 千、10 万、100 万行；更大规模分批执行并显式配置预算。
- 指标：encode/decode rows/s 与 MiB/s、耗时分布、输出字节数、峰值 RSS；支持时补充运行时堆指标。
- 区分核心编解码时间、文件 I/O、进程启动、跨语言传输和验证成本。
- 固定种子、构建模式、硬件、OS、工具链和依赖版本；预热后多次运行，保存原始数据。
- 核心回归集同机中位数下降超过 15% 时要求解释和复核；该阈值用于人工复核，不能把波动较大的共享 CI 直接作为速度判决。
- 与 PyArrow 的比较只在口径一致时展示；不以 Python 进程总时长对比 MoonBit 纯函数耗时。

## 7. 风险与范围调整

| 风险 | 应对与触发条件 |
| --- | --- |
| 赛程短于估算 | 先完成 P0–P3，并重新评估剩余必选任务；不能把未完成字典/增量能力标成支持，4000 行目标需单独复核 |
| 嵌套或字典导致 API 大改 | 先完成数据模型决策和最小外部 fixture，再扩展；版本说明记录破坏性变更 |
| 四后端数值差异 | 以 UInt64、Float32 位模式和时间边界作为优先测试，拒绝经 JS Number 丢精度 |
| 字典占用持续增长 | 独立字典预算、明确生命周期；stream 更新与 file 读取分别测试 |
| 增量接口仍积累完整输入 | 增加长批次序列内存实验，检查已消费缓冲释放及消费者持有结果的影响 |
| 依赖或工具链升级 | 保留固定可复现基线，在独立升级通道验证后再切换 |
| 代码规模达标但质量不足 | 逐模块审查实际调用、重复代码、文档、错误用例与外部兼容证据 |
| 扩展任务拖延交付 | 先冻结必选范围，压缩/Decimal/Map 等按实际需求另排，不挤占核心验证时间 |

## 8. 参赛交付包与第一轮执行清单

交付包应包含：源码版本、核心行数报告、API 文档、格式支持矩阵、架构及设计取舍、CI 链接或实际执行记录、互操作报告、基准原始结果、独立消费项目、两个演示工作流、已知限制和后续路线。新增第三方语料和依赖同时记录来源与许可。

参赛叙述围绕三个可演示结果展开：MoonBit 能与主流数据工具交换数据；下游能直接复用而无需重写编解码；错误输入与大批次处理有明确行为和证据。行数报告作为工作量证据之一单独提供。

下一轮从 P0 开始，按以下顺序执行：

- [ ] 固定计数口径并生成机器可读基线。
- [ ] 重跑 PyArrow 双向互操作，更新验证记录，保留失败样本。
- [ ] 为当前实现采集第一份性能基线。
- [ ] 整理 API 所有权、兼容性与错误契约，列出 P1 的具体接口变化。
- [ ] 完成工具链与 CI 的可复现性检查。
- [ ] 实现第一组新增整数类型，贯通类型 → metadata → buffer → 外部 fixture → 文档的完整流程。

与原 `docs/ROADMAP.zh-CN.md` 的关系：原文保留项目定位，本文件给出详细执行方案。功能实施时同步两处状态，避免路线图与实际支持范围产生矛盾。

## 9. 依据与后续查阅入口

本地依据：`moon.mod`、`moon.pkg`、五个核心源码文件、`pkg.generated.mbti`、现有测试、`tools/interop.py`、`.github/workflows/ci.yml`、`docs/FORMAT.md`、`docs/VALIDATION.md` 和 `docs/ROADMAP.zh-CN.md`。

已于编制日期查阅官方 [Arrow 列式格式与 IPC 规范](https://arrow.apache.org/docs/format/Columnar.html) 以及 [Arrow 集成测试说明](https://arrow.apache.org/docs/format/Integration.html)。类型布局、字典状态与集成验证在实施时应继续逐项对照规范；项目自己的测试通过不等于通过完整官方集成测试。

协议元数据还需对照官方 [Schema.fbs](https://github.com/apache/arrow/blob/main/format/Schema.fbs)、[Message.fbs](https://github.com/apache/arrow/blob/main/format/Message.fbs) 和 [File.fbs](https://github.com/apache/arrow/blob/main/format/File.fbs)；实施时记录所依据的修订版本，避免依赖可变的 main 分支作为唯一复现依据。
